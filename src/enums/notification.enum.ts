
export enum Notification_Enum {
    LIKE = 'like',
    COMMENT = 'comment',
    FOLLOW = 'follow',
}

export enum Notification_Object_Type_Enum {
    REEL = 'reel',
    USER = 'user',
    POST = 'post',
}
