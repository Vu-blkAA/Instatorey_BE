
export enum Gender_Enum {
    MALE = 'male',
    FEMALE = 'female',
    OTHER = 'other',
}


export enum Follow_Status_Enum {
   PENDING = 'pending',
   ACCEPTED = 'accepted',
   REJECTED = 'rejected',
}